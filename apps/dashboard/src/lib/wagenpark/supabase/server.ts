/**
 * Supabase server-client — gebruikt in Server Components en Server Actions.
 * Delegeert naar @everts/database/server voor consistentie over alle apps.
 */
export { createClient } from '@everts/database/server'
